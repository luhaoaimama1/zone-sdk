//todo 数据自动完成 对于有动画的那种怎么办？  我觉得动画写在adapter 也不错


接口完事  逻辑基本好使就行  细节问题需要大量的测试



------------------//todo 未完成的问题!!!!!!--------------------------------


------------------//todo 问题转接!!!!!!--------------------------------
当上啦加载的时候 数据没返回回来  在触发上啦加载的问题
（这个应 该在 下拉刷新框架中处理  处理的方法是：当加载的时候  能在加载 需要设置加载完成 方可继续）

listView框架 需要 全局改变动画方法

-----------//todo 暂时不做的-----------------
非异步请求处

缓存问题（如果要做缓存需要 mapp需要排序 或者直接用okhttp那个~我觉得后者省里  只需要添加一个过滤器）

cookie问题

默认返回码 问题

大文件下载 应该是独立的一部分 不属于这里 所以暂时就不封装了

//下面这两个不知道什么时候能用到
private static final  String Response_Charset="utf-8";
private static final String Params_Charset="utf-8";

----------//todo 需要问的问题------------
(问问群里  什么时候异常可以 安静的处理  什么的异常不管 即使app关闭了)

----------//todo 已经完成了的任务------------
pullview应该又 加载更多和  下拉刷新的接口

数据请求失败的时候  点击会重新请求  这种情况 翻页自动回退一页既保持不变  不然会多番一夜

把adatper下拉刷新 和 上啦记载  集合到这里！！！！！！！！！！！！！

http框架做的:异常检查    全局配置

cancel tag  与 tag的退出方法  context~

异常检测

list关联上  dialog不显示

logutils 打印log日志

ExceptionUtils 这个被调用的时候需要打印 调用此方法的 的位置  这样可以快速找到错误的位置

如果到最后一页(既数据量<Limit) 就不请求了 不然 有的listView还显示上啦 等待的 进度条之类的

上啦加载  返回pageNumber  如果 刷新 pageNumber为0   在这里不行！！！因为没成功 也会+1所以还是需要在我net框架中处理
(这样的话 接口貌似可以去掉  handler传入的时候可以 传个页数 arg1  arg2不暂时不用故-1 )

接口onSuccess 加个tag  分  内存缓存  磁盘缓存  http

https 网络框架全局处理的说

send的修改 build 模式( 这是为了以后的扩展 仅仅是为了保存参数)  但是三个参数那个快可以留着

与listView关联后 tag 可以不设 既-1 这个时候 在内部处理了就  如果在handler中也处理点东西那么就设置tag

-------------//todo 最后要做的--------------------
RequestParamsNet  这个先这样 最后需要把  requestParams摘出去  不然耦合的太严重了